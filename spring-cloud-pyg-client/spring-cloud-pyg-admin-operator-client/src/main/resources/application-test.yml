spring:
  application:
    name: spring-cloud-pyg-admin-operator-client
server:
  port:  8600
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
